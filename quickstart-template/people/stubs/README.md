# Stubs

A holding pen for people you only need to mention but aren't ready to write up -
a name on a census line, someone you'll place later. A stub can be as short as a
name. Move it into a numbered folder (an ancestor) or into `connections/` once you
know where they belong.
