# documents/

Put your scans, certificates, clippings, and transcripts here (PDFs, images, text).
If you'd rather keep them somewhere else (an external drive, an existing folder),
you can delete this folder and point `fha.yaml` at the real location instead.
