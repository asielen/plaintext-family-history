<!-- This template is retired (#76) - a stub and a curated person are one
     shape now, not two: every section a curated record can have, a stub has
     too (just typically empty until you research it). There is no longer a
     separate, smaller "stub" template to copy.

     Copy ../_TEMPLATE.person.md instead - it already defaults to
     `tier: stub`, so a plain copy of it IS a stub. Nothing else about that
     template changes for a stub vs. a curated record; only how much of it
     you have filled in does. -->
