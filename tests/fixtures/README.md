# tests/fixtures/

Broken fixtures for individual lint codes go here (e.g. `broken-E003-filename-id-mismatch/`), created alongside the linter implementation.
The clean fixture is `../../example-archive/`.
